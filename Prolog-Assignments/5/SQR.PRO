DOMAINS
	A, A1 = INTEGER
	
PREDICATES
	SQUARE(INTEGER)
	
CLAUSES
	SQUARE(A) :-	A1 = A * A,
			WRITE(A, "^2 = ", A1) AND NL.

GOAL
	WRITE("ENTER A NUMBER : ") AND
	READINT(A) AND
	SQUARE(A).