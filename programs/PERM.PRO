DOMAINS
	X = INTEGER
	Y = INTEGER*
	
PREDICATES
	PERMUTE(Y,Y)
	DELNUM(INTEGER,Y,Y)
	
CLAUSES
	DELNUM(X,[X|T],T).
	DELNUM(X,[H|T],[H|T1]):- DELNUM(X,T,T1).
	
	PERMUTE([],[]).
	PERMUTE([H|T],PL):- 	PERMUTE(T,PL),
				DELNUM(H,PL,T).